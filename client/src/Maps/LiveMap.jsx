import React from 'react';

// This map is used to show the live location in the Dasboards
const LiveMap = () => {
    return (
        <div>
            
        </div>
    );
}

export default LiveMap;
